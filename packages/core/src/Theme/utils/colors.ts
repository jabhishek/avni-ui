import { Colors } from './types';

const primary = '#1471eb';
const white = '#fff';
const black = '#000';
const textBlack = '#111';

export const colors: Colors = {
  primary,
  white,
  black,
  textBlack,
};
